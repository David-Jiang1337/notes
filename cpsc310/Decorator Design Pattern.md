Sometimes we wish to make a class that is extended with many different types of functionality, and might need functionality from multiple different subclasses. Instead of creating a subclass for each permutation of functionalities, we can use a decorator design pattern so that an abstract class is extended by the original class, alongside a decorator class. Additional functionality can then be written as extensions of the decorator. In short, decorators should be used when we need optional, combinable behavior in our class.